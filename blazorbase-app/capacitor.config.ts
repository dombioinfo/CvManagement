import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'blazorbase.app',
  appName: 'blazorbase-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
